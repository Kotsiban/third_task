--
Ќапишите простую реализацию функции дл€ разбора параметров командной строки (getopt).
ѕолагаем, что нам требуетс€ структура данных, содержаща€ заранее известный допустимый набор параметров различного типа - строки, целые, числа с плавающей точкой и т. д., а также признак, €вл€етс€ ли этот параметр об€зательным.
ѕолагаем, что параметры могут передаватьс€ только в "длинном" формате с об€зательным "--" перед именем параметра (то есть "--my-option-name my-option-val", "--my-option-name=my-option-val", "--my-boolean-option").
ѕараметров может быть несколько.
‘ункци€ должна об€зательно уметь обрабатывать параметр "--help" (если он указан в списке параметров при вызове функции), вывод€щий пример использовани€ (необ€зательные параметры должны выводитьс€ в квадратных скобках).
--
try this:
--help --long --long=123 --long 412 --int --int=412 --int 234 --int --plot --float --float=1244
